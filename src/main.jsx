import React from "react";
import ReactDOM from "react-dom/client";
import RandyMotors from "./RandyMotors";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RandyMotors />
  </React.StrictMode>
);
